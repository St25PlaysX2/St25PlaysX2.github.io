bash -c "(sleep 30 && while true; do if ! bluetoothctl info 0F:80:0D:23:60:F8 | grep -q 'Connected: yes'; then bluetoothctl connect 0F:80:0D:23:60:F8 && sleep 5 && pactl set-default-sink \$(pactl list short sinks | grep bluez | awk '{print \$2}'); fi; sleep 60; done) &"
